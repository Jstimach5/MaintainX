/**
 * The app's single icon system: a curated re-export of lucide-react.
 * Import icons from here (not from lucide-react directly) so the set in
 * use stays visible in one place and swaps stay one-file changes.
 * Icon-only controls must carry an aria-label; decorative icons are
 * aria-hidden by lucide's default (they render as plain <svg>).
 */
export {
  // Navigation & shell
  ArrowLeft,
  Home,
  Bell,
  ChevronDown,
  ChevronRight,
  LogOut,
  Menu,
  User,
  Users,
  Settings,
  // Work
  Wrench,
  ClipboardList,
  ClipboardCheck,
  CalendarDays,
  BarChart3,
  Timer,
  ListChecks,
  // Maintenance
  Gauge,
  Repeat2,
  // Assets & places
  Boxes,
  Box,
  MapPin,
  Building2,
  QrCode,
  FileUp,
  ScrollText,
  // Actions & feedback
  Plus,
  Search,
  Camera,
  ImagePlus,
  MessageSquare,
  Printer,
  Pencil,
  Archive,
  X,
  Check,
  CheckCircle2,
  TriangleAlert,
  Info,
  CircleAlert,
  Loader2,
  Inbox,
} from "lucide-react";
